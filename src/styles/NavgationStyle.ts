import styled from 'styled-components';

export const NavgationStyle = styled.nav`
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: center;
    a {
        color: white;
        text-decoration: none;
        padding: 10px;
    }
`;
